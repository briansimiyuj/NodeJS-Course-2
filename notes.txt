Let's say we had a route to `/about`, but we changed our mind and decided to change it to `/about-us`. We can redirect users from the old route to the new one. We need to add a new `/about-us` and set the status code to 301, which means permanent redirect. then we `setHeader` to `Location` and set it to `/about-us`. Finally, we `end` the response.

video #4