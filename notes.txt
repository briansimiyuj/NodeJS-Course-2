Whenever we make a change to the server file, we need to restart the server to see the changes.
The request object contains information about the request that was made to the server. For now let's just log `request.url` and `request.method` to the console.
The log printed `/` and `GET` to the console. The `/` is the root URL of the server and `GET` is the HTTP method

video #4