Sometimes we need to send dynamic data to the client. This is where templating or view engines come in. `EJS` is one of the most popular view engines for Node.js. Let's install it.
    `npm install ejs`

video #7