Let's now retreive blogs from the database. We'll create a new route to "/all-blogs", then use `find` method on `Blog` model to get all blogs from the database. Then we'll send the blogs as a response.

video #9